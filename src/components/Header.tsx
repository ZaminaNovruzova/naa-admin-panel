const Header = () => {
  return (
    <header
      className="header sticky h-full bg-amber-100 w-xs z-<1000>
"
    >
      <nav className="navBar">Posts</nav>
    </header>
  );
};

export default Header;
